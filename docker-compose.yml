services:
  ritl:
    build: .
    container_name: ritl
    volumes:
      - .:/app
    ports:
      - "5561:5561"
      - "5562:5562"
    extra_hosts:
      - "host.docker.internal:host-gateway"    # lets container reach host on Linux
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - ritl-net

networks:
  ritl-net:
    driver: bridge